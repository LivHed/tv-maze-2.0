import React from 'react';
import { render, screen } from '@testing-library/react';
import App from './App';

//figure out the console error when trying to run this basic first test. why jest 'finds an unexpected token' and why 'Jest failed to parse a file'.

/*
test('header renders', () => {
  render(<App />);
  const header = screen.getByText(/tvmaze/i);
  expect(header).toBeInTheDocument();
}); */
